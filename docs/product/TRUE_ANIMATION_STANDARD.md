# True Animation Standard

Motion-comic previews can use zooms, pans, overlays, and keyframe transitions. Final output requires character bodies, limbs, antagonists, cloth/scarf/ribbon elements, and dialogue timing to move inside the scene. Reject the output if it is just a still image sliding around.
