# lookBOOK v0.2.0 — True Animation Standard

This release locks lookBOOK into its core positioning: **Not a slideshow. Real animation planning.**

Try: `lookbook demo my-demo` then `lookbook true-animation-packet my-demo --target runway`.
