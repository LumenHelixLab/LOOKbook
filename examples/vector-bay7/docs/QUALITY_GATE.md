# Quality Gate

Accept only if characters move inside the frame, enemies react physically, scarf/ribbon motion is independent, and camera motion does not substitute for character animation. Reject if it is only zoom/pan, particles, or static keyframes sliding around.
